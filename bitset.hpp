# ifndef __bitset__hpp__
# define __bitset__hpp__
# include <iostream>
class bitset
{
    public :
        bitset operator<<(int unsigned( __shift_amount ) )
        {

        }

        bitset operator>>(int unsigned( __shift_amount ) )
        {

        }
       
        bitset(int unsigned(__bitset_length ))
        {
            (this-> bitset_list ) = new int unsigned [__bitset_length];
        }
    private :
        int unsigned(* bitset_list);
} ;

# endif /*__bitset__hpp__*/
